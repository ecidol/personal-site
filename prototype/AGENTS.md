# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

Keep a local-only hero media editor available during development: clicking the hero background opens its media card, shows the source specifications, and lets the owner replace `public/assets/video/intro.mp4` with an MP4. Do not expose this editor in production builds.

The first `The project` background panel no longer shows its previous image information/replacement, frosted-glass undo, or deleted-copy undo buttons. It now shows only one local-only `更换背景视频（MP4 / MOV）` button, accepts MP4 or MOV files up to 200 MB, preserves the selected video across refreshes, fills the panel with muted looping autoplay, and falls back to the existing group image when no video is selected. MOV playback still requires a browser-supported codec. Per the user's explicit exception, this specific change and future video replacements through this button do not have an undo button. Do not expose this editor in production builds.

The first `The project` background panel keeps its current stored frosted-glass state, but its old frosted-glass undo control is no longer displayed.

The `01.` label and English paragraph on the first `The project` panel are hidden, and their old restore button is no longer displayed.

The former project panel `01`, including its current background video and local video replacement button, is moved to the third screen from the end: immediately before the bottom video panel, followed by that video panel and then the contact/footer screen. Keep one local-only position undo on the moved panel that restores only its original location.

A full-height blank panel is inserted immediately after project panel `01` and before panel `02`; its current background is black after the previous white-background change was undone. Its five previous controls—panel insertion undo, white-background undo, video upload, video replacement undo, and video-feature undo—are currently hidden together. Keep one local-only `撤销本次删除按钮` control that restores only those five controls. Any already selected background video and the blank panel itself remain unchanged while the controls are hidden.

The blank panel has a separate local-only background image upload/replace feature that remains visible even while its five older controls stay hidden. Accept JPG, PNG, WebP, or AVIF images up to 25 MB, persist the chosen image across refreshes, and cover the full panel. Keep one image-replacement undo for the latest upload and one feature-specific undo for the newly added background-image capability; neither may restore the five older hidden controls.

That first blank panel is currently hidden at the user's request. Preserve its stored background image/video and all related state instead of deleting those assets. Keep one local-only `撤销本次删除上一屏` control on the following designed panel that restores only this hidden screen.

A second full-height black blank panel is inserted immediately below the first blank panel and immediately above the original project panel `02`. Keep one local-only insertion undo on the new panel that removes only this newly inserted blank page.

That second panel now uses the user-supplied AMAN reference as its layout source: a full-bleed warm luxury-interior background, a large warm-ivory editorial card containing a portrait resort image and Chinese copy, plus a large vertical `AMAN` wordmark at the right edge. The title, body, and vertical word are editable together in local preview. The card image and full-page background each have their own local-only upload/replace control. Keep four operation-scoped one-step undos: the current layout back to the prior black blank panel, the latest text edit, the latest card-image replacement, and the latest full-page-background replacement. These controls must not undo the original insertion or any unrelated change.

The second panel's full-bleed background currently has a 14px frosted-glass blur with a subtle warm-white veil. The ivory card, its image and copy, the vertical wordmark, and all local editor controls remain sharp above the effect. Keep one local-only `撤销本次毛玻璃` button that restores only the previously sharp background.

The complete editable `about-lower-blank-panel` screen, currently headed `动态8K电影截帧`, is moved up by exactly one visible screen, swapping places with the immediately preceding Cinematic List screen. Preserve the article ID, copy, uploaded content image, uploaded page background, frosted-glass effect, and every existing editor/undo control. Keep one separate local-only `撤销本次上移一屏` control on the moved screen that restores only its former position below the Cinematic List.

A new full-height black blank screen is inserted immediately below the frosted editable panel and immediately above the original project panel `02`. Keep one local-only `撤销本次插入空白屏` control on that new screen that removes only this insertion.

That new lower screen now uses the supplied lake-restoration timeline reference as its layout source: a full-bleed warm mountain/lake background, centered bilingual heading with replaceable waveform marks, a replaceable seven-node dashed timeline graphic, seven Chinese milestone blocks, and a bottom transparent frosted-glass mask whose opacity deepens toward the bottom. The heading, subtitle, and all seven milestone titles/descriptions are editable together in local preview. The full-page background, timeline graphic, and title mark each have their own local-only upload/replace and latest-change undo controls. Keep one separate local-only layout undo that restores only the prior black blank screen, without removing the screen itself or changing unrelated pages.

A new independent full-height black video page is inserted immediately below the lake-restoration timeline screen. It accepts locally selected video files across common formats through a `video/*` upload/replace control, stores files up to 200 MB, fills the screen with muted looping autoplay when the browser can decode the selected format, and clearly notes that playback support depends on browser codecs. Keep one latest-video replacement undo and one separate insertion undo that removes only this new page without changing the timeline or any following screen.

The project-panel content set includes the original `02` and `03` panels plus three additional copies of that two-panel group, numbered `04` through `09`, with one local-only undo that removes all three added groups without changing panels `01`–`03`. Preserve the saved content and editor state even when individual panels are hidden from the visible flow.

The English paragraph on each project panel `02` through `09` is independently editable through a local-preview modal and persists across refreshes. Keep a separate one-step latest-text undo for every panel so undoing one screen never changes another screen's confirmed copy.

Project panels `02` through `09` are currently removed from the visible page flow. The latest operation removed only the selected panels `08` and `09`, while the earlier deletion of panels `02`–`07` remains in effect. Preserve every panel's saved text, backgrounds, video, and feature state. The immediately preceding shooting-equipment folder page carries one local-only `撤销本次删除第 08、09 屏` control that restores only panels `08` and `09` to the still-deleted `02`–`07` state without changing any other page.

The two full-screen English Impact introduction sections (`section#impact` and the immediately following `.projects-intro`) are currently removed together from the visible page flow. Preserve their copy, image, link, styling, and original order in code. Keep one local-only `撤销本次删除这两屏` control on the following podcast introduction that restores only these two sections, and move the `#impact` anchor to that following visible section while the pair is hidden so the primary navigation remains functional.

Two full-height black blank screens follow the newly inserted timeline video page and remain before the project-panel sequence. They are separate explicit DOM nodes with unique IDs, unique dedicated class names, separate persisted visibility state, and separate local-only insertion undos. Selecting, annotating, editing, or undoing one screen must never target or change the other.

The first of those two independent screens now follows the supplied `IMG_4394.jpg` Aman core-values reference: a 29/71 split with a full-height forest image and real AMAN mark on the left, plus a warm-ivory editorial content field with a bilingual heading, English/Chinese principles, four value rows, two stacked detail images, and a bottom English note. All visible copy is editable together in local preview. The left background and both detail images each have their own local-only upload/replace control and latest-change undo. Keep a separate layout undo that restores only this first screen to black, while leaving its original insertion state and the second independent blank screen unchanged.

The Aman core-values screen includes a fifth value row copied directly beneath `人文温度`, with the exact same three-column alignment, typography, and 16px row gap as the original four rows. This copied row has its own local-only `编辑复制行` modal, persisted title/body content, latest-copy-edit undo, and insertion undo. Editing or removing the copied row must never change the original four value rows or any other Aman copy.

The complete Aman core-values screen is moved down by exactly one screen, swapping places with the immediately following film-poster screen. Keep the Aman screen as one explicit independent article with the same ID and annotation target, preserve every content/edit/upload/undo feature inside it, and keep one separate local-only position undo that restores only the former order.

The raster `AMAN` mark formerly overlaid on the left-side image of the Aman core-values screen is currently hidden. Keep one separate local-only undo that restores only this mark without changing the left background image, the editorial content, or any other screen.

The complete Aman core-values screen is currently removed from the visible page flow. Its content and prior local edit state remain intact for recovery. Keep one separate local-only removal undo on the following black blank screen that restores only the Aman screen in its former position without changing any neighboring screen.

The second independent screen now follows the supplied `IMG_4397.jpg` film-poster reference: a centered wide black poster with an oversized editable Bebas Neue title, a replaceable monochrome cinematic portrait, editable left-side genre/director credits, and an editable right-side studio lockup with the source's real raster circular mark. The portrait and the full screen background each have their own local-only upload/replace control and latest-change undo. Keep one separate layout undo that restores only this second screen to black, and preserve its original insertion undo separately. The first Aman screen must remain unaffected by every edit, upload, annotation, and undo on this screen.

The film-poster screen and all six `Impact initiatives` project-card screens (`project-1` through `project-6`) are currently removed from the visible flow as one selected seven-page operation. Preserve all poster copy, images, backgrounds, project data, and prior editor state. Keep one local-only `撤销本次删除选中七页` control on the following Market page that restores only these seven selected pages.

A third full-height black blank page is inserted immediately below the second independent film-poster screen and immediately before project panel `02`. It is a separate explicit DOM node with its own unique ID, dedicated class name, persisted visibility state, annotation target, status, and local-only insertion undo. Selecting, annotating, or undoing this page must never target or change either preceding independent screen.

The third independent page now follows the supplied `IMG_4412.jpg` market reference: a near-even split with a clean white editorial field on the left and a full-height street photograph on the right. The left field contains an oversized `MARKET` heading, a small editable category label, and three editable lines. All visible text is editable together in local preview, and the right photograph has its own local-only upload/replace control and latest-image undo. Keep a separate layout undo that restores only this page to its prior black blank state; preserve the page insertion undo and the separate Aman-page removal undo.

A fourth full-height page with a pure white background was inserted immediately below the third black blank page and immediately before project panel `02`, but is currently removed from the visible page flow at the user's request. It remains a separate explicit DOM node with its own unique ID, class, persisted visibility state, annotation target, status, and retained content. Its removal must not affect the dark editorial screen above or any neighboring page. Keep one local-only `撤销本次删除 ARTHUR 页面` control on the following expanding-card screen that restores only this page.

That fourth page now follows the supplied `IMG_4416.jpg` House/NEST reference: a pure-white full-screen editorial composition with three small top labels, a largely empty center, a bottom-left architecture photograph, edge metadata, and an oversized `NEST` word filling the bottom-right region. All six visible text fields are editable together in local preview, and the architecture photograph has its own local-only upload/replace control and latest-image undo. Keep a separate layout undo that restores only this fourth page to its prior pure-white blank state; preserve the page insertion undo independently.

The fourth House/NEST page's oversized editable word automatically compresses horizontally according to its character count so the complete current title remains visible inside the right-hand region. Keep one local-only `撤销本次完整标题显示` control that restores only the prior oversized width without changing the saved word or any other page content.

A new independent full-height screen is inserted immediately above the current ARTHUR profile screen (`about-timeline-blank-04`). It is a separate explicit article with its own unique ID, annotation target, persisted visibility state, status, and local-only `撤销本次新增空白屏` control. Selecting, annotating, or undoing this screen must never target or change the ARTHUR screen or any neighboring page. This screen now follows the supplied `IMG_4449.jpg` reference: a deep charcoal editorial composition with an oversized condensed wordmark on the left, a compact lower-center copy block, a small caption above a tall right-hand portrait, and intentionally generous negative space. All four visible text fields are editable together in local preview. The oversized editable wordmark automatically compresses horizontally according to its current character count so the complete title remains visible in the left-hand region; keep a local-only `撤销本次完整标题显示` control that restores its previous width without changing the saved text. The right portrait has its own local-only JPG/PNG/WebP/AVIF upload-or-replace control and latest-image undo; its generated default asset is `public/assets/images/timeline-blank-eight-editorial-portrait.png`. Keep a separate layout undo that restores only this screen to its prior pure-white blank state, while preserving independent title-fit, text, image, and insertion undo scopes.

A fifth full-height independent blank page was inserted immediately below the House/NEST screen and immediately before project panel `02`, but is currently removed from the visible page flow at the user's request. Preserve its uploaded group photo, background editor, and prior local state. Keep one local-only `撤销本次删除此屏` control on the following seventh black blank page that restores only this fifth screen without changing the House/NEST screen, expanding-card page, seventh blank page, or project panel `02`.

A sixth full-height independent black blank page is inserted immediately below the fifth image-background page and immediately before project panel `02`. It has its own unique article ID, dedicated class, annotation target, persisted visibility state, status, and local-only insertion undo. Selecting, annotating, or undoing this sixth page must never target or change the fifth page or project panel `02`.

That sixth page now contains a responsive seven-card expanding gallery. On desktop it expands horizontally on hover, focus, or click; on mobile it expands vertically. Every collapsed thumbnail and its expanded state use the exact same image source. The page heading and every card title/description are locally editable and deletable, every Lucide card mark can be changed or hidden, and every card image has its own upload/replace control and per-card latest-image undo. Keep a separate latest text/mark undo, a layout undo that restores only the black blank screen, and the existing page-insertion undo. Do not migrate the rest of this established JSX/CSS prototype to Tailwind or TypeScript solely for this component; keep the reusable component in `src/components/ui` and use the existing `src/styles.css` design system.

The complete sixth expanding-card page is moved up by exactly one screen, swapping places with the immediately preceding fifth group-photo background page. Preserve both pages as independent article elements with their existing IDs, annotation targets, content, uploads, and undo tools. Keep one local-only `撤销本次上移一屏` control on the expanding-card page that restores only the previous page order.

A seventh full-height independent page remains immediately below the fifth group-photo background page and immediately before project panel `02` after the expanding-card page moves upward. It now contains the adapted interactive folder gallery titled `拍摄设备`, using the five user-supplied equipment photographs in their provided order; the four HEIC originals are converted to browser-compatible JPG assets without changing their source order, and the fifth JPG remains the Canon camera image. Clicking or focusing the folder opens the same five photographs in a responsive fan, dragging a photograph downward or pressing Escape closes it, and the compact layout remains usable on narrow screens. Keep a layout-only local undo that returns this page to its immediately previous black blank state, plus the page's independent insertion undo and the existing control that can restore the removed fifth screen. It has its own unique ID, dedicated class, annotation target, persisted visibility state, and status. Selecting, annotating, or undoing it must not affect the expanding-card screen, the group-photo page, or project panel `02`. Do not migrate the rest of this established JSX/CSS prototype to Tailwind or TypeScript solely for this component; keep the reusable gallery in `src/components/ui` and use `src/styles.css`.

An independent full-height pure-black blank screen is inserted immediately below the shooting-equipment folder page and before the remaining Impact content. It has the unique article ID `about-timeline-gallery-followup-blank-panel`, its own annotation target, persisted visibility state, status, and a local-only `撤销本次新增空白屏` control. Removing or restoring this screen must not change the folder gallery, its equipment images, the deletion state of panels `02`–`09`, or any following Impact content.

That shooting-equipment follow-up screen now contains a responsive two-card pricing layout adapted from the supplied shadcn component and stored in `src/components/ui/pricing-cards.jsx`. The `Get started` and `Contact sales` CTA blocks are intentionally omitted in full. Every visible pricing string—tier name, currency, price, interval, description, and all twelve feature labels—is repeatedly editable through one local modal, with included-state checkboxes, persisted JSON content, and a latest-text undo. Keep the layout-only undo separate so it restores the prior black blank screen without deleting saved pricing copy or undoing the screen insertion. This established Vite/JSX/CSS site must not be migrated to Tailwind or TypeScript solely for this component; use the existing `src/components/ui` path, `src/styles.css`, and installed `lucide-react` dependency.

The `DISCOVER THE PROJECT` buttons are currently hidden from panels `03`, `05`, `07`, and `09`. Keep a local-only one-step undo on panel `03` that restores those four buttons without changing the panels themselves.

The original project panel `02` has a local-only MP4 background upload/replace control. Without an uploaded video it retains the existing mountain image; uploaded video fills the panel with muted looping autoplay while the existing `02.` label and copy remain sharp above it. Keep a video-specific one-step undo and a separate feature-specific undo. The copied panels `04`, `06`, and `08` are unaffected.

The footer's social links, newsletter form, and legal/language row are currently hidden, and the footer height collapses accordingly while retaining the large wordmark. Keep a local-only one-step undo in the footer that restores only those three selected areas.

The full-screen English contact invitation (`footer#join > .footer__invitation`) is currently removed from the page flow at the user's request. Preserve its exact copy and contact link in code, retain the large footer wordmark below it, collapse the otherwise empty footer height, and keep a separate local-only `撤销本次删除此屏` control that restores only the contact invitation without restoring the already hidden social, newsletter, or legal areas.

The oversized footer wordmark reads `JURNEOS` instead of `ICEBERG`. Keep a local-only one-step undo beside the wordmark that restores only this text.

A full-screen black video panel is positioned immediately before the footer/contact screen, one screen above its original after-footer position. In local preview it has an MP4 upload/replace button, a video-specific one-step undo, a position-specific undo that moves it back after the footer, and a separate one-step undo that removes only the newly added panel. Uploaded video fills the panel, autoplays muted, and loops; the editing controls stay local-only.

For every future user-requested website modification, retain the immediately previous state and add a clearly labeled local-only “撤销本次修改” control scoped to that operation, unless the user explicitly says that operation does not need an undo control. A single undo must never revert unrelated confirmed changes.

The large statement screen formerly rendered at `#the-project` is currently removed from the visible page flow at the user's request. Preserve its supplied Chinese copy, bundled OFL-licensed Smiley Sans treatment, local editing state, and all existing text/edit-feature histories in code. Keep a zero-height `#the-project` navigation anchor at its former position and a separate local-only `撤销本次删除此屏` control on the immediately following Scroll Fly-In screen that restores only this statement screen.

An additional independent full-height blank screen is inserted immediately below `#the-project`; its current background is pure white. It has the unique section ID `statement-followup-blank-panel-02`, its own annotation target, persisted visibility and appearance state, and status. Keep one local-only background undo that restores only its previous black background and a separate `撤销本次新增空白屏` control that removes only this new screen. Neither undo may alter the statement or the existing image screen that follows it.

That white screen now contains four responsive Blur Vignette video cards adapted from the official UI Layouts registry source. Preserve the source interaction exactly: each card autoplays muted and loops, uses a 24px radius with a 10px vignette inset, 100px transition length, and 15px edge blur, and scales its video from 1 to 1.1 on hover or keyboard focus. Desktop uses a two-by-two grid; narrow screens use one full-height card at a time in a horizontal snap row. The bundled default video is the locally stored source clip at `public/assets/video/blur-vignette-forest.mp4`; never hotlink the remote demo asset. Each card has its own local-only video upload/replace control accepting MP4, MOV, WebM, or OGV up to 200 MB and its own latest-video undo. Each of the four cards also has its own repeated-use text editor for eyebrow, title, and description, with one latest-card-text undo shared across the four-card operation. Keep a separate layout-only undo that restores this screen to the prior plain white state without changing the screen insertion or white-background state. Do not migrate this established JSX/CSS prototype to Tailwind or TypeScript solely for this registry component; keep the reusable adaptation at `src/components/ui/blur-vignette.jsx` and style it through `src/styles.css`.

The complete four-card Blur Vignette screen is currently removed from the visible flow at the user's request. Preserve all four uploaded videos, card copy, layout settings, background state, and existing undo histories. Keep one local-only `撤销本次删除视频卡片页` control on the immediately following image screen that restores only this removed screen without changing either screen's content or any neighboring page.

A full-height pure-white screen with the unique section ID `statement-followup-white-panel-03` now follows the newest pure-black blank screen and remains above the currently removed Blur Vignette screen. It has its own annotation target, persisted visibility state, and a local-only `撤销本次新增白屏` control that removes only this screen. The removed Blur Vignette screen, its uploaded videos, and the following image screen remain unchanged.

Another independent screen is now inserted immediately below the large `#the-project` statement and immediately before the Cinematic List screen. Its background remains pure white, and it now contains the supplied 200vh Scroll Fly-In hero interaction adapted to this established Vite/JSX/CSS project at `src/components/ui/hero-section-3.jsx`: the copy stays centered in a sticky viewport while the plane image travels from far left to far right and fades at the scroll-range edges. Its two visible strings are editable together through one local modal, persist across refreshes, and have a latest-copy undo. Keep the layout-only `撤销本次滚动飞入排版`, background-only `撤销本次白色背景`, and screen-only `撤销本次新增空白屏` histories separate. The screen retains the unique section ID `statement-followup-blank-panel-04` and must not change the statement, Cinematic List content, or any other inserted/removed page.

The Scroll Fly-In main heading is currently forced into exactly two authored lines with a uniform responsive font size: `你已经习惯自己解决一切，` followed by `可是人生若一直独行再美的风景也少了些特别`. Preserve the explicit line break and keep both lines the same size. Keep one local-only `撤销本次两行排版` control that restores only the prior heading text and automatic wrapping without changing the eyebrow, Scroll Fly-In component, background, screen insertion, or unrelated text history.

The `statement-followup-white-panel-03` screen now contains the four-row Cinematic List adapted from the supplied shadcn registry component. Preserve the source interaction: each desktop row grows from 96px to 400px on hover or keyboard focus using the Quartz easing curve, its background image fades and scales from 1.1 to 1, the title shifts upward, the location reveals, and the circular arrow turns white and rotates -45 degrees. Mobile rows remain fixed-height with the location below the title. Keep the reusable Vite/JSX adaptation at `src/components/ui/cinematic-list.jsx` and the styling in `src/styles.css`; do not add Next.js or migrate this established site to Tailwind/TypeScript solely for this component. The page header and each row's visible copy are repeatedly editable and locally persisted with one latest-text undo. Each of the four rows has its own JPG/PNG/WebP/AVIF upload/replace control, persisted image metadata, and independent latest-image undo. Keep the layout-only undo separate so it restores the pure-white blank page without undoing the page insertion, text, or uploaded images.

The previously inserted independent full-height image screen with section ID `statement-followup-blank-panel` is currently deleted from the page flow. Preserve its persisted visibility state, current uploaded image, local upload metadata, latest-image undo, and insertion undo so no content is lost. The Cinematic List page immediately above it provides a separate local-only `撤销本次删除图片页` control that restores only this deleted screen. Restoring or deleting it must not change the Cinematic List, the previously removed Blur Vignette screen, or any following page.

The two bottom descriptive paragraphs in the split `THE PODCAST` and `THE CROWDFUNDING` media cards are currently hidden together at the user's request. Preserve their exact copy in code and keep empty layout spacers so the headings retain their established positions. Keep a separate local-only `撤销本次删除文字` control that restores only these two paragraphs without changing either card's title, imagery, tint, or action.

The `Coming soon` status pill in `THE PODCAST` and the complete `VIEW MORE` round link in `THE CROWDFUNDING` are currently hidden together. Preserve both saved labels and the crowdfunding URL so they can be restored. Keep one separate local-only `撤销本次删除两个按钮` control that restores only these two actions without changing either card's headings, backgrounds, bottom-paragraph visibility, or text-edit history.

The user-supplied `8973.JPG` MACO WeChat QR image is inserted as a centered, uncropped content image in the previously empty middle area of the left `THE PODCAST`/MACO card. Use the bundled local asset at `public/assets/images/media-card-maco-qr.jpg`, preserve its complete white canvas and QR code, and scale it responsively without changing the card heading, tint, or the right card. Keep one separate local-only `撤销本次插入图片` control that hides only this image and leaves the retained asset and every other media-card history unchanged.

The user-supplied `8975.JPG` ARTHUR WeChat QR image is inserted as a centered, uncropped content image in the previously empty middle area of the right `THE CROWDFUNDING`/ARTHUR card. Use the bundled local asset at `public/assets/images/media-card-arthur-qr.jpg`, preserve its complete white canvas and QR code, and apply the same responsive sizing rules as the MACO image without changing either card heading, tint, or the left image. Keep a separate local-only `撤销本次插入图片` control on the right card that hides only this image and leaves the retained asset and every other media-card history unchanged.

The visible copy in the `ECHOES OF CHANGE` introduction and both split media cards is editable from local-only controls. The introduction editor covers its heading and body; each card editor covers its title prefix, main title, status/action label, and its bottom body if that body has been restored. All values persist in `src/content/impact-media-copy.json` and share one latest-copy undo that never changes the separate two-paragraph visibility state. Keep a distinct `撤销本次新增文字编辑` control for removing only these newly added edit entries.

All five visible Chinese lines in the large `#the-project` statement are editable together through one local-preview modal. Saving persists the five lines across refreshes. Keep one latest-text undo scoped only to these five lines and one separate feature undo that removes only the newly added edit entry without changing the confirmed copy or the statement's visual treatment.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

The user's latest standing preference supersedes all earlier requirements to expose local edit, upload, history, or undo controls in the webpage. Keep `editorEnabled` disabled in local preview as well as production so no operation-specific buttons, file inputs, editor modals, status badges, or undo controls appear on the site. Preserve confirmed content, assets, persisted state, and prior history files in the codebase, but apply all future requested changes directly in the project without adding webpage controls. This does not remove intentional visitor-facing interactions or ordinary navigation links that are part of the designed site content.
